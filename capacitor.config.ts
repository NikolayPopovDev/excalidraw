import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iExcalidraw.app',
  appName: 'iExcalidraw',
  webDir: 'excalidraw-app/build'
};

export default config;
